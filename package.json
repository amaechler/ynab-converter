{
  "name": "ynab-csv-converter",
  "version": "1.0.0",
  "description": "Convert bank CSV to YNAB-compatible format",
  "private": true,
  "scripts": {
    "build": "tsc && cp index.html styles.css dist/",
    "deploy": "npm run build && gh-pages -d dist",
    "dev": "tsc --watch",
    "format": "prettier --write \"**/*.{ts,js,html,css,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,js,html,css,json,md}\"",
    "lint": "eslint \"src/**/*.ts\"",
    "lint:fix": "eslint \"src/**/*.ts\" --fix"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.19.0",
    "@typescript-eslint/parser": "^6.19.0",
    "eslint": "^8.56.0",
    "gh-pages": "^6.1.1",
    "prettier": "^3.2.4",
    "typescript": "^5.3.3"
  }
}
